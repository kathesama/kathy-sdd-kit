# /debt-harvest

Standalone changelog-only command. Scans ticket-scoped files for
`sdd-simplification:` markers and appends a debt ledger entry to the ticket
changelog. Does not modify implementation files. `close-ticket-workflow` also
runs this automatically when ticket-scoped markers are detected.

Invoke: `ai-specs/skills/debt-harvest/SKILL.md`
